import javax.management.Query;

/*
    This class is intended to build and
     form SQL queries dynamically.

     QueryBuilder always returns instance of self and
     should be outputed via toString(); method where it's constructed and returned.

     This is intended so methods can be chained as in example below of
     getting all properties, from labin, where price is less then 500
     QuerryBuilder(new House().table_name)->where('

 */
public class QueryBuilder {

}
